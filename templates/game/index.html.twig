{% extends 'base.html.twig' %}

{% block title %}Partie en cours | Code Secret{% endblock %}

{% block body %}
    <section class="w-full min-w-[300px] max-w-[1280px] m-7 p-3 sm:p-7 border-solid border-[3px] border-white rounded-xl">
        <div class="lg:flex">
            {{ component('GameEnterCode', {'codeToDisplay': codeToDisplay}) }}
            {{ component('GameDisplay', {'journal' : journal}) }}
        </div>
    </section>
{% endblock %}
